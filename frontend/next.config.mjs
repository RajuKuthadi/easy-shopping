/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.pinimg.com', 'img.freepik.com',
            'media.istockphoto.com', 'rukminim2.flixcart.com'
        ], // Add your image domains here
    },
};

export default nextConfig;
